import 'package:flutter/material.dart';

class Elephant_death_places extends StatefulWidget {
  @override
  _Elephant_death_placesState createState() => _Elephant_death_placesState();
}

class _Elephant_death_placesState extends State<Elephant_death_places> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Posts'),
        backgroundColor:Color(0xFF03AC13).withOpacity(.20),
      ),
    );
  }
}
